<body>
    <p>This is the index page</p>
    <p> You are logged in as {{user.username}}</p>
  <!-- Load most recent polls -->
  {{#if polls}}
  <p>Polls</p>
  {{polls}}
  {{else}}
  <p>No polls loaded</p>
  {{/if}}
</body>